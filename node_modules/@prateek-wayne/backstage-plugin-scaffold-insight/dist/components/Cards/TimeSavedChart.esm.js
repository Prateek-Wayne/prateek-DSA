import React from 'react';
import { Card, CardContent, Box, Typography, Divider } from '@mui/material';
import { BarChart } from '@mui/x-charts/BarChart';
import { BarChartOutlined } from '@material-ui/icons';

const TimeSavedChart = ({
  chartData
}) => /* @__PURE__ */ React.createElement(Card, { elevation: 3 }, /* @__PURE__ */ React.createElement(CardContent, null, /* @__PURE__ */ React.createElement(Box, { display: "flex", alignItems: "center", gap: 2, mb: 2 }, /* @__PURE__ */ React.createElement(BarChartOutlined, null), /* @__PURE__ */ React.createElement(Typography, { variant: "h6" }, "Time Saved per Template")), /* @__PURE__ */ React.createElement(Divider, { sx: { mb: 2 } }), /* @__PURE__ */ React.createElement(Box, { height: 300 }, /* @__PURE__ */ React.createElement(
  BarChart,
  {
    xAxis: [
      { scaleType: "band", data: chartData.map((item) => item.name) }
    ],
    series: [
      {
        data: chartData.map(
          (item) => parseInt(item.timeSaved.value || "0")
        )
      }
    ],
    width: 700,
    height: 300
  }
))));

export { TimeSavedChart };
//# sourceMappingURL=TimeSavedChart.esm.js.map
