import { createPlugin, createApiFactory, discoveryApiRef, configApiRef, identityApiRef, fetchApiRef, createRoutableExtension } from '@backstage/core-plugin-api';
import { rootRouteRef, entityRootRouteRef } from './routes.esm.js';
import { scaffolderAnalyticsApiRef, ScaffolderAnalyticsClient } from './api/api.esm.js';

const scaffolderAnalyticsPlugin = createPlugin({
  id: "scaffolderanalytics",
  routes: {
    root: rootRouteRef
  },
  apis: [
    createApiFactory({
      api: scaffolderAnalyticsApiRef,
      deps: {
        discoveryApi: discoveryApiRef,
        configApi: configApiRef,
        identityApi: identityApiRef,
        fetchApi: fetchApiRef
      },
      factory: ({ discoveryApi, identityApi, fetchApi }) => {
        return new ScaffolderAnalyticsClient({
          discoveryApi,
          identityApi,
          fetchApi
        });
      }
    })
  ]
});
const ScaffolderAnalyticsPage = scaffolderAnalyticsPlugin.provide(
  createRoutableExtension({
    name: "ScaffolderanalyticsPage",
    component: () => import('./components/ScaffolderAnalytics/index.esm.js').then(
      (m) => m.AnalyticsDashBoard
    ),
    mountPoint: entityRootRouteRef
  })
);

export { ScaffolderAnalyticsPage, scaffolderAnalyticsPlugin };
//# sourceMappingURL=plugin.esm.js.map
